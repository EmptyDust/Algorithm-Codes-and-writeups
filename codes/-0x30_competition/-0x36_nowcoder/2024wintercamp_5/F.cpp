#include <bits/stdc++.h>
#define int long long 
#define op first
#define num second
using namespace std;
constexpr int MAXN = 2e5 + 10, mod = 1e9 + 7;
int nums[MAXN];
int b[MAXN][7], pre[MAXN][7];
using pt = pair<int, int>;
int n, k;

signed main() {
    ios::sync_with_stdio(false);
    int n;cin >> n;
    for (int i = 1;i <= n;++i) {
        cin >> nums[i];
    }

    return 0;
}